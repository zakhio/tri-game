import { defineMessages } from 'react-intl';

const messages = defineMessages({
    playAnotherOne: {
        id: 'page.game.nextRound',
        defaultMessage: 'Play Another',
        description: 'Button on start game session page',
    },
});

export default messages;
