import { defineMessages } from 'react-intl';

const messages = defineMessages({
    text: {
        id: 'feature.end.message',
        defaultMessage: 'End of the game!',
        description: 'Message when game is ended',
    },
});

export default messages;
